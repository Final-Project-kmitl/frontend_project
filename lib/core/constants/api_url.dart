class AppUrl {
  static const baseUrl = 'https://catfact.ninja/fact';

  static const signUp = "api/v1/auth/signup";

  // check product routine in user add
  static const routine_check_product = "${baseUrl}/routine-product";

  // check no match in routine
  static const routine_check_no_match = "${baseUrl}/check-no-match";
}
